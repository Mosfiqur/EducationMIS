export interface ILcMapQueryModel {
    selectedCamps: number[];
    selectedIPs: number[];
    selectedPPs: number[];
    selectedUnions: number[];
    selectedUpazilas: number[];
    selectedTPs: number[];   
}
